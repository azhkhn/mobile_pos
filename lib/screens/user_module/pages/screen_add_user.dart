import 'package:flutter/material.dart';

class ScreenAddUser extends StatelessWidget {
  const ScreenAddUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
